name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Cache node_modules
        uses: actions/cache@v5
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ matrix.node-version }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-${{ matrix.node-version }}-

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

      - name: Run tests with coverage (shard 1/5)
        run: npx vitest run --coverage --shard=1/5
        env:
          NODE_OPTIONS: "--max-old-space-size=4096"

      - name: Run tests (shard 2/5)
        run: npx vitest run --shard=2/5
        env:
          NODE_OPTIONS: "--max-old-space-size=4096"

      - name: Run tests (shard 3/5)
        run: npx vitest run --shard=3/5
        env:
          NODE_OPTIONS: "--max-old-space-size=4096"

      - name: Run tests (shard 4/5)
        run: npx vitest run --shard=4/5
        env:
          NODE_OPTIONS: "--max-old-space-size=4096"

      - name: Run tests (shard 5/5)
        run: npx vitest run --shard=5/5
        env:
          NODE_OPTIONS: "--max-old-space-size=4096"

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/lcov.info
          fail_ci_if_error: false
          verbose: true

      - name: Build project
        run: npm run build
